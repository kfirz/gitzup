# Binaries for programs and plugins
*.so
/buildagent
/api-server
/resource-*
/webhooks-server

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Imports from dep ensure
/vendor

# Local files
/*.local.*
